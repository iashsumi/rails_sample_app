- content_for(:html_title) { 'ユーザー新規登録確認' }

= form_with(model: @user, url: users_path, method: :post, local: true) do |f| 
  = render :partial => 'users/confirm', :locals => {:f => f}
  = f.submit '登録'
