- content_for(:html_title) { 'ユーザー編集確認' }

= form_with(model: @user, url: user_path(@user), method: :PATCH, local: true) do |f| 
  = render :partial => 'users/confirm', :locals => {:f => f}
  = f.submit '更新'
